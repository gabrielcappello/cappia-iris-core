# Spec — Novo agendamento

**Status:** não escrita ainda. Este arquivo é um placeholder.

Esta especificação deve detalhar o fluxo de novo agendamento (identificação → aproveitar
dados em qualquer ordem → disponibilidade → cadastro necessário → resumo → confirmação
explícita → criação idempotente → resposta), conforme `../docs/06-roadmap.md`, a partir
das decisões em `../docs/04-decisoes-canonicas.md`.

Não preencher por suposição. Escrever somente depois que Gabriel definir e aprovar o
comportamento detalhado (ver processo em `../AGENTS.md`).
