{
  "name": "cappia-iris-core",
  "private": true,
  "type": "module",
  "description": "Core deterministico da Iris Nova (TypeScript). Todo o codigo do repositorio cappia-iris-core vive aqui dentro.",
  "scripts": {
    "test": "node --test core/**/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0"
  },
  "devDependencies": {
    "@types/node": "24.13.3",
    "typescript": "7.0.2"
  }
}
